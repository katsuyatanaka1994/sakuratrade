# AUTO-GENERATED FILE
# このファイルは自動生成されます。直接編集しないでください。
components:
  responses:
    problem_response:
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/problem'
      description: Error
    problem_response_401:
      description: Unauthorized
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/problem'
    problem_response_403:
      description: Forbidden
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/problem'
    problem_response_404:
      description: Not Found
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/problem'
  parameters:
    trade_id:
      in: path
      name: trade_id
      required: true
      schema:
        type: integer
  schemas:
    alert:
      properties:
        alertId:
          type: integer
        targetPrice:
          type: number
        tradeId:
          type: integer
        triggeredAt:
          format: date-time
          type:
          - string
          - 'null'
        type:
          enum:
          - TP
          - SL
          type: string
      required:
      - alertId
      - tradeId
      - type
      - targetPrice
      type: object
    image:
      properties:
        imageId:
          type: integer
        s3Url:
          type: string
        thumbnailUrl:
          type: string
        tradeId:
          type: integer
        uploadedAt:
          format: date-time
          type: string
      required:
      - imageId
      - tradeId
      - s3Url
      - uploadedAt
      type: object
    pattern_result:
      properties:
        advice:
          type: string
        diagnosedAt:
          format: date-time
          type: string
        patternId:
          type: integer
        rule:
          type: string
        score:
          format: float
          type: number
        tradeId:
          type: integer
      required:
      - patternId
      - tradeId
      - rule
      - score
      - diagnosedAt
      type: object
    problem:
      properties:
        detail:
          type: string
        instance:
          type: string
        status:
          type: integer
        title:
          type: string
        type:
          type: string
      required:
      - type
      - title
      - status
      type: object
    trade:
      properties:
        enteredAt:
          format: date-time
          type: string
        exitedAt:
          format: date-time
          type:
          - string
          - 'null'
        priceIn:
          type: number
        priceOut:
          type:
          - number
          - 'null'
        side:
          enum:
          - LONG
          - SHORT
          type: string
        size:
          type: number
        ticker:
          type: string
        tradeId:
          type: integer
        userId:
          format: uuid
          type: string
      required:
      - tradeId
      - userId
      - ticker
      - side
      - priceIn
      - size
      - enteredAt
      type: object
    user:
      properties:
        email:
          format: email
          type: string
        plan:
          type: string
        role:
          type: string
        userId:
          format: uuid
          type: string
      required:
      - userId
      - email
      type: object
  securitySchemes:
    bearer_auth:
      bearerFormat: JWT
      scheme: bearer
      type: http
openapi: 3.1.0
info:
  description: REST API for sakuratrade (auth, trades, alerts, images, patterns, health)
  x-revision: 2025-10-19 smoke
  x-docsync-smoke: true
  x-smoke: ds-18.1 test (remove after smoke test)
  title: sakuratrade API
  version: 0.0.1
tags:
- name: auth
  description: User authentication and registration
- name: trades
  description: Trade resources and summaries
- name: alerts
  description: Price alert resources for trades
- name: images
  description: Chart image uploads and presigned URLs
- name: patterns
  description: Pattern diagnostics and scoring
- name: health
  description: Service status and readiness
paths:
  /alerts:
    post:
      summary: Create alert
      tags:
      - alerts
      operationId: createAlert
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/alert'
        required: true
      responses:
        '201':
          description: Created alert
          headers:
            Location:
              description: URL of the created resource
              schema:
                type: string
                format: uri
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/alert'
        '401':
          $ref: '#/components/responses/problem_response_401'
        default:
          $ref: '#/components/responses/problem_response'
  /alerts/{trade_id}:
    get:
      summary: List alerts for trade
      tags:
      - alerts
      operationId: listTradeAlerts
      parameters:
      - $ref: '#/components/parameters/trade_id'
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/alert'
                type: array
          description: Alert list
        '404':
          $ref: '#/components/responses/problem_response_404'
        '401':
          $ref: '#/components/responses/problem_response_401'
        default:
          $ref: '#/components/responses/problem_response'
  /auth/oauth:
    post:
      summary: Authenticate via OAuth
      tags:
      - auth
      operationId: oauthLogin
      requestBody:
        content:
          application/json:
            schema:
              properties:
                provider_token:
                  type: string
              required:
              - provider_token
              type: object
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                properties:
                  token:
                    type: string
                type: object
          description: JWT token
        default:
          $ref: '#/components/responses/problem_response'
      security: []
  /auth/register:
    post:
      summary: Register user account
      tags:
      - auth
      operationId: register
      requestBody:
        content:
          application/json:
            schema:
              properties:
                email:
                  format: email
                  type: string
                password:
                  type: string
              required:
              - email
              - password
              type: object
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/user'
          description: Registered
        default:
          $ref: '#/components/responses/problem_response'
      security: []
  /health:
    get:
      summary: Check service health
      tags:
      - health
      operationId: getHealth
      responses:
        '200':
          description: OK
  /images:
    post:
      summary: Request image upload URL
      tags:
      - images
      operationId: requestImageUpload
      requestBody:
        content:
          application/json:
            schema:
              properties:
                tradeId:
                  type: integer
              required:
              - tradeId
              type: object
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                properties:
                  s3Url:
                    type: string
                type: object
          description: Pre-signed URL
        default:
          $ref: '#/components/responses/problem_response'
  /patterns/{trade_id}:
    get:
      summary: Get pattern result for trade
      tags:
      - patterns
      operationId: getPatternResult
      parameters:
      - $ref: '#/components/parameters/trade_id'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pattern_result'
          description: Pattern result
        '404':
          $ref: '#/components/responses/problem_response_404'
        '401':
          $ref: '#/components/responses/problem_response_401'
        default:
          $ref: '#/components/responses/problem_response'
  /trades:
    post:
      summary: Create trade
      tags:
      - trades
      operationId: createTrade
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/trade'
        required: true
      responses:
        '201':
          description: Created trade
          headers:
            Location:
              description: URL of the created resource
              schema:
                type: string
                format: uri
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/trade'
        '401':
          $ref: '#/components/responses/problem_response_401'
        default:
          $ref: '#/components/responses/problem_response'
  /trades/{trade_id}:
    get:
      summary: Get trade bundle
      tags:
      - trades
      operationId: getTradeBundle
      parameters:
      - $ref: '#/components/parameters/trade_id'
      responses:
        '200':
          content:
            application/json:
              schema:
                properties:
                  pattern_result:
                    $ref: '#/components/schemas/pattern_result'
                  trade:
                    $ref: '#/components/schemas/trade'
                type: object
          description: Trade data
        '404':
          $ref: '#/components/responses/problem_response_404'
        '401':
          $ref: '#/components/responses/problem_response_401'
        default:
          $ref: '#/components/responses/problem_response'
security:
- bearer_auth: []
servers:
- description: Internal API
  url: /api/v1
